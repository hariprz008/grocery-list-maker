This is the grocery list maker 🛒🛒 which is completely build by python programming language. We can add the entire grocery item like solid and liquid, It is built to separately ask the unit. 

🛒
✨SPECIAL ABOUT IT:
          ➡️ Grocety list maker code which handle the ❌errors perfectly because of using the try and except block more specifically.
          ➡️ File handling is the important part of this programm, It take the output(grocery list) and write into the text file which is mentioned in the program.
          ➡️ Item name,quantity,unit are the three inputs which asks the user to give. Those are in the while true loop to execute infinitely, When the user breaks the logic of the part of code might be the code breaks.
          ➡️ specifically in unit part of the code, There is a function used which is called enumerator, Which is used in for loop along with the list. When iteration occurs enumerator gives the index also.
